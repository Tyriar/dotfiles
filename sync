#!/bin/bash

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

cp -f $DIR/.gitconfig ~
cp -f $DIR/.vimrc ~
cp -f $DIR/.bash_profile ~

$DIR/sync-helpers/gnome-terminal-profile.sh
$DIR/sync-helpers/sublime-seti-theme.sh
$DIR/sync-helpers/sublime-preferences.sh
